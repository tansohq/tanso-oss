databaseChangeLog:
  - changeSet:
      id: 20260321-1
      author: kat
      comment: "Default features to enabled — new features should be active by default"
      changes:
        - sql:
            sql: ALTER TABLE features ALTER COLUMN is_enabled SET DEFAULT true
