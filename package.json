{
  "name": "tanso-developer-workspace",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "workspaces": [
    "sdks/typescript",
    "examples/nextjs-ai-credits"
  ],
  "scripts": {
    "build": "npm run build --workspace @tansohq/sdk && npm run build --workspace @tansohq/nextjs-ai-credits-example",
    "check": "npm run check --workspace @tansohq/sdk && npm run check --workspace @tansohq/nextjs-ai-credits-example",
    "test": "npm run test --workspace @tansohq/sdk"
  }
}
